<Window x:Class="Anastasia423WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Читай, Пиши и не спиши" Height="650" Width="1100"
        WindowStartupLocation="CenterScreen" Background="#F5EFE6">

    <Window.Resources>
        <Style x:Key="NavBtn" TargetType="Button">
            <Setter Property="Background"       Value="Transparent"/>
            <Setter Property="BorderThickness"  Value="0"/>
            <Setter Property="Foreground"       Value="#C8B89A"/>
            <Setter Property="Cursor"           Value="Hand"/>
            <Setter Property="Height"           Value="52"/>
            <Setter Property="Padding"          Value="16,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent" BorderThickness="0,0,0,3" BorderBrush="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#28FFFFFF"/>
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#C8A882"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="56"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#3D2B1F">
            <Grid Margin="12,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock Text="📖" FontSize="24" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Читай, Пиши" FontSize="15" FontWeight="Bold" Foreground="#E8D5B7" FontFamily="Georgia" LineHeight="18"/>
                        <TextBlock Text="и не спиши" FontSize="10" Foreground="#A08060" FontFamily="Georgia"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal"
                            HorizontalAlignment="Center" VerticalAlignment="Stretch">
                    <Button x:Name="BtnCatalog" Style="{StaticResource NavBtn}" Click="BtnCatalog_Click" ToolTip="Каталог книг">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="🔍" FontSize="16" HorizontalAlignment="Center" Foreground="#C8A882"/>
                            <TextBlock Text="Каталог" FontSize="11" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnLists" Style="{StaticResource NavBtn}" Click="BtnLists_Click" ToolTip="Списки чтения">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="📚" FontSize="16" HorizontalAlignment="Center"/>
                            <TextBlock Text="Мои списки" FontSize="11" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnAuthor" Style="{StaticResource NavBtn}" Click="BtnAuthor_Click" ToolTip="Мои публикации" Visibility="Collapsed">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="✍" FontSize="16" HorizontalAlignment="Center"/>
                            <TextBlock Text="Мои книги" FontSize="11" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnAdmin" Style="{StaticResource NavBtn}" Click="BtnAdmin_Click" ToolTip="Панель администратора" Visibility="Collapsed">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="⚙" FontSize="16" HorizontalAlignment="Center" Foreground="#C8A882"/>
                            <TextBlock Text="Управление" FontSize="11" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
                <Button Grid.Column="2" x:Name="BtnProfile" Cursor="Hand" Click="BtnProfile_Click" ToolTip="Личный кабинет" Height="40" Padding="14,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="#5C3D2E" CornerRadius="20"
                                    Padding="{TemplateBinding Padding}">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Border Width="26" Height="26" CornerRadius="13"
                                            Background="#E8D5B7" Margin="0,0,8,0">
                                        <TextBlock Text="👤" FontSize="13"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                    <TextBlock x:Name="TxtProfile" Text="Профиль"
                                               Foreground="#E8D5B7" FontSize="13"
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#3D2B1F"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
        <Frame x:Name="MainFrame" Grid.Row="1"
               NavigationUIVisibility="Hidden" Background="#F5EFE6"/>
    </Grid>
</Window>
