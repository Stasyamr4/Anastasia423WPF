<Page x:Class="Anastasia423WPF.NewFolder1.Products"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Anastasia423WPF.NewFolder1"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Products">

    <Grid>
        <ListBox x:Name="listBox" 
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ItemsSource="{Binding Path=products}" d:ItemsSource="{d:SampleData ItemCount=5}">

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Background="#FF672E2E" Margin="10" Width="350" Height="300">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>


                        <Image Source="{Binding ImagePath}" 
                               Grid.Row="0"
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"
                               Width="221"
                               Height="145"/>

                        <StackPanel Grid.Row="1" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center"
                    Margin="10">
                            <TextBlock Text="{Binding Name}" 
                       HorizontalAlignment="Center" 
                       TextWrapping="Wrap"
                       FontSize="16" 
                       Foreground="#FFD0ACAC"/>
                            <TextBlock Text="{Binding Price, StringFormat='Цена: {0:C}'}" 
                       HorizontalAlignment="Center"
                       FontSize="14" 
                       Foreground="#FFE0C0C0"
                       Margin="0,5,0,0"/>
                            <Button x:Name="ToBasket" Content="Добавить в корзину" Margin="10"></Button>
                        </StackPanel>

                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>

            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <Button x:Name="NextPage" Content="Далее" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="30" Width="100" FontSize="16" Click="NextPage_Click"></Button>
    </Grid>
</Page>
